# Validator expectations for the IMG functional annotation dialect,
# model/dialects/img-functional-gff.yaml.
profile: img-functional-gff/1.0.0
dialect: model/dialects/img-functional-gff.yaml
scope: Validator expectations for the retained NMDC and JGI isolate IMG functional annotation files.

# Independent validator observations; reproduce with just validity-check.
validation:
  scope: Exact rule sets for the listed retained corpus inputs, including negative controls. These are
    validator observations, not conversion-support claims.
  genometools:
    status: measured
    expected_failures:
      missing-version: 'The required first-line ##gff-version directive is absent. GenomeTools stops here;
        later defects are not measured.'
  gff3toolkit:
    status: measured
    expected_failures:
      Esf0003: QC reports strand . as missing on the unstranded CRISPR and repeat_unit rows of the
        JGI isolate file.
      Esf0014: 'The first line lacks ##gff-version. The source is preserved without adding it.'
      Esf0032: One Prodigal CDS in the JGI isolate file repeats the shortened key.
      Esf0036: QC reports literal commas in attributes, such as product names. This is recorded as a
        tool result, not a dialect prohibition.
  agat:
    status: blocked
    expected_failures: null
    reason: No measured rule list. See analyses/format-validation/agat-blocker.json for the 2026-09-25
      macOS arm64 dependency failure.
  cases:
    nmdc-functional-annotation:
      genometools:
      - missing-version
      gff3toolkit:
      - Esf0014
    jgi-img-clostridium-functional-annotation:
      genometools:
      - missing-version
      gff3toolkit:
      - Esf0003
      - Esf0014
      - Esf0032
      - Esf0036
